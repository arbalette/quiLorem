// Initialize an array
let params = [];

// Assume selection is a variable holding some value
let selection = "newItem";

// Use the push method to add the value of selection to the params array
params.push(selection);

// The params array now contains the value "newItem"
console.log(params); // Output: ["newItem"]
